# The native shell loads the built files from tauri://localhost, which is a
# different origin from the service — so unlike the dev-server build it cannot
# use a proxy and must address the API directly. The bearer token comes from
# the Rust side, which reads it off disk.
VITE_API_BASE=http://127.0.0.1:39215/api
